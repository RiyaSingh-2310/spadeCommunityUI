# API origin (defaults to http://localhost:5050 when unset)
VITE_API_BASE_URL=http://localhost:5050

# Optional Bearer sent on POST /api/admin/login (only if your backend requires it)
VITE_API_LOGIN_BEARER_TOKEN=
