# API base URL (include /api suffix)
VITE_API_BASE_URL=http://localhost:5050/api

# Dev-only: log API request URL, payload, and response (true/false)
VITE_API_DEBUG=false

# Optional Bearer sent on login + forgot-password / verify-otp / reset-password
VITE_API_LOGIN_BEARER_TOKEN=

# Default ISO phone country for PhoneInput (e.g. IN, US, GB)
VITE_DEFAULT_PHONE_COUNTRY=IN

# Optional public base for questionnaire group links in admin listings
# e.g. https://your-app-host/public/questionnaire
# Defaults to current origin + /public/questionnaire when unset
VITE_QUESTIONNAIRE_GROUP_URL_BASE=

# Optional panelist id appended to Website URL links in Questionnaire Group listing
# Final format: /public/questionnaire/{groupId}?panelist_id={panelistId}
VITE_QUESTIONNAIRE_GROUP_PANELIST_ID=
